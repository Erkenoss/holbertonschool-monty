#include "monty.h"

void get_func(FILE *file)
{
    instruction_t get[] = {
        {"push", push},
        {"pall", pall},
        {NULL, NULL}
    }
    int iteration = 0;

    while (get[iteration].opcode != NULL)
    {
        
    }
}